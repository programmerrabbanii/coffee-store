const Card = ({coffee}) => {
    const {id,name,image,category,ingredients,popularity,rating,type,origin}=coffee
    return (
        <div>
            <h4>{name}</h4>
            
        </div>
    );
};

export default Card;